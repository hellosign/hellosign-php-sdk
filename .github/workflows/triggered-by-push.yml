name: triggered-by-push
on:
  push:
    branches:
      - openapi

jobs:
  run-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - run: ./build