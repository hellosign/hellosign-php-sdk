name: github-actions
on:
  push:
    branches:
      - openapi
  pull_request:
    branches:
      - openapi

jobs:
  run-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - run: ./build